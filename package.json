{
  "name": "coliseum-shop",
  "version": "1.0.0",
  "description": "API de integração com Mercado Pago",
  "main": "server.js",
  "type": "module", // Adicione para usar ES Modules (opcional)
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2", // Recomendo voltar para a v4 (mais estável)
    "mercadopago": "^2.8.0",
    "firebase-admin": "^11.11.0" // Adicione se usar Firebase
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0" // Atualize para Node 18+ (suporte a ES Modules)
  }
}